#include <stdio.h>
#include <stdlib.h>
// #include <stdbool.h>
#include <string.h>

int main(){
    int a;
    scanf("Num=%d",&a);
    printf("%d", a);
    // char line[255];
    // FILE * fpointer = fopen("employee.txt", "r");

    // fgets(line, 255, fpointer);
    // fgets(line, 255, fpointer);

    // printf("%s", line);

    // fclose(fpointer);
    printf("\n");
    return 0;
}
